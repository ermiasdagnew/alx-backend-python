# 0x03. Unittests and Integration Tests

## Project Overview

This project contains exercises on writing **unit tests**, **integration tests**, and using **mocking** with Python’s `unittest` framework.

We focus on testing:
- Utility functions (`utils.py`)
- API clients (`client.py`)
- Properly using `patch` and `parameterized`
- Testing memoized properties and exception handling

---

## File Structure

